/**
 * `hifis.app` 루트 — **아직 안 정했다.**
 *
 * 지금 이 사이트에 있는 것은 매장에 붙는 두 화면뿐이다
 * (`/survey/{token}` · `/tv/{token}`). 루트에 무엇을 둘지는 정해지면 만든다
 * (회사 소개 · 앱 받는 곳 · 아무것도 안 두기 — 셋 다 후보다).
 */
export default function Home() {
  return null;
}
